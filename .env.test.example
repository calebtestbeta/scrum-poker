# Firebase 測試配置範例
# 複製此檔案為 .env.test 並填入您的 Firebase 專案資訊
# 注意：.env.test 已加入 .gitignore，不會提交到版本控制

# Firebase 專案 ID
# 位置：Firebase 控制台 > 專案設定 > 一般 > 專案 ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Web API Key
# 位置：Firebase 控制台 > 專案設定 > 一般 > 網頁 API 金鑰
FIREBASE_API_KEY=AIzaSyD...your-firebase-api-key

# 可選：Firebase 資料庫 URL（如果使用自訂 URL）
# FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com/

# 可選：Firebase Auth Domain（如果使用自訂域名）
# FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# 測試配置
# 測試超時時間（毫秒）
TEST_TIMEOUT=30000

# 測試房間 ID 前綴（避免與生產環境衝突）
TEST_ROOM_PREFIX=e2e-test-